namespace CardBuilder
{
    /// <summary>
    /// Keeps track of which panel is active of activeElementPanel
    /// </summary>
    public enum PanelType
    {
        //For properties
        Property,
        PropertyEnum,

        //For TreeView
        Image,
        Number,
        Text,
        None,
        Root
    }
}
